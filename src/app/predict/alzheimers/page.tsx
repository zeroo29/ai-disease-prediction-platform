import { AppShell } from "@/components/app-shell";import { PredictionWorkspace } from "@/components/prediction-workspace";export const metadata={title:"Alzheimer’s risk analysis"};export default function Page(){return <AppShell title="Alzheimer’s risk analysis" subtitle="Run a validated, versioned demo-model inference using non-identifiable research inputs."><PredictionWorkspace/></AppShell>}