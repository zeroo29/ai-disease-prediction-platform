import { currentSession,unauthorized } from "@/lib/auth";import { ok,validationError } from "@/lib/api";import { predictionSchema,infer } from "@/lib/ml";import { z } from "zod";export async function POST(req:Request){if(!await currentSession())return unauthorized();try{const body=predictionSchema.parse(await req.json());return ok({contributions:infer(body.features).explanation,disclaimer:"Feature contribution indicates how the model used the input. It does not establish medical causation."});}catch(e){if(e instanceof z.ZodError)return validationError(e);throw e;}}