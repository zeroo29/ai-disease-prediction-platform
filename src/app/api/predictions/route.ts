import { db } from "@/db";import { predictions } from "@/db/schema";import { desc,eq } from "drizzle-orm";import { currentSession,unauthorized } from "@/lib/auth";import { ok,fail,validationError } from "@/lib/api";import { infer,MODEL,predictionSchema } from "@/lib/ml";import { z } from "zod";
export async function GET(){const s=await currentSession();if(!s)return unauthorized();const rows=await db.select().from(predictions).where(eq(predictions.userId,s.userId)).orderBy(desc(predictions.createdAt));return ok(rows);}
export async function POST(req:Request){const s=await currentSession();if(!s)return unauthorized();try{const body=predictionSchema.parse(await req.json());if(MODEL.status!=="available")return fail("MODEL_UNAVAILABLE","The selected model is not available.",503);const result=infer(body.features);const [row]=await db.insert(predictions).values({userId:s.userId,modelName:MODEL.name,modelVersion:MODEL.version,modelChecksum:MODEL.checksum,inputData:body.features,prediction:result.prediction,probability:result.probability,explanation:result.explanation}).returning();return ok(row,"Prediction generated successfully");}catch(e){if(e instanceof z.ZodError)return validationError(e);return fail("SERVER_ERROR","Prediction could not be generated.",500)}}