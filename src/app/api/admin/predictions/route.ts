import { db } from "@/db";import { predictions,users } from "@/db/schema";import { desc,eq } from "drizzle-orm";import { currentSession,unauthorized,forbidden } from "@/lib/auth";import { ok } from "@/lib/api";export async function GET(){const s=await currentSession();if(!s)return unauthorized();if(s.role!=="admin")return forbidden();const rows=await db.select({id:predictions.id,userId:predictions.userId,userEmail:users.email,modelName:predictions.modelName,modelVersion:predictions.modelVersion,prediction:predictions.prediction,createdAt:predictions.createdAt}).from(predictions).innerJoin(users,eq(predictions.userId,users.id)).orderBy(desc(predictions.createdAt)).limit(100);return ok(rows);}