import { db } from "@/db";import { users } from "@/db/schema";import { desc } from "drizzle-orm";import { currentSession,unauthorized,forbidden } from "@/lib/auth";import { ok } from "@/lib/api";export async function GET(){const s=await currentSession();if(!s)return unauthorized();if(s.role!=="admin")return forbidden();const rows=await db.select({id:users.id,fullName:users.fullName,email:users.email,role:users.role,isActive:users.isActive,createdAt:users.createdAt}).from(users).orderBy(desc(users.createdAt));return ok(rows);}