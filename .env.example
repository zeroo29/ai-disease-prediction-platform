DATABASE_URL=postgresql://postgres:postgres@localhost:5432/medpredict
JWT_SECRET=replace-with-at-least-32-random-characters
DEMO_MODE=true
# Optional password-reset email transport
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
# Optional development seed identities; never use these defaults in production
SEED_ADMIN_EMAIL=admin@example.test
SEED_ADMIN_PASSWORD=
SEED_DEMO_EMAIL=demo@example.test
SEED_DEMO_PASSWORD=
